import axios from 'axios'
import router from '../router'
// Hier könnten alle Requests von den Vue-Komponenten an den Server reinkommen, die
// irgendwelche Daten senden oder auf dem Server verändern wollen (POST,PUT ...)

const API_URL = 'http://localhost:3000/'

class Auth {

}

export default new Auth()
